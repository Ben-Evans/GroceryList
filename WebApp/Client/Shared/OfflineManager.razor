@*@if (_isOnline)
{
    <MudIcon Icon="@Icons.Material.Filled.Check" Color="Color.Success" Title="Online" />
}
else
{
    <MudIcon Icon="@Icons.Material.Filled.Error" Color="Color.Error" Title="Offline" />
}

@code {
    [Inject] IOfflineManagerService OfflineManagerService { get; set; }

    protected override async Task OnInitializedAsync()
    {
        OfflineManagerService.NetworkConnectionStatusChanged += ;
        await OfflineManagerService.CheckNetworkConnectionAsync();
    }
}*@
